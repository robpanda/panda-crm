{
  "name": "panda-crm-daily-sync",
  "version": "1.0.0",
  "description": "Daily Salesforce to PostgreSQL sync Lambda",
  "main": "index.js",
  "scripts": {
    "build": "npm install && zip -r function.zip .",
    "deploy": "aws lambda update-function-code --function-name panda-crm-daily-sync --zip-file fileb://function.zip"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.0.0",
    "@aws-sdk/client-secrets-manager": "^3.0.0",
    "@prisma/client": "^5.7.0",
    "jsforce": "^1.11.0"
  },
  "devDependencies": {
    "prisma": "^5.7.0"
  }
}
